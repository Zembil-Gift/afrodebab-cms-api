package com.afrodebab.cms.jpa.repository;

import com.afrodebab.cms.jpa.entity.SupportMetric;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SupportMetricRepository extends JpaRepository<SupportMetric, Long> {
}
